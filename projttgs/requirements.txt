# Core Django framework - Django 5.0+ required for Python 3.13 support
# (Django 3.2 doesn't support Python 3.13 due to removed cgi module)
Django>=5.0,<6.0

# PDF generation
xhtml2pdf==0.2.11

# Image processing - using version with pre-built wheels for Python 3.13
Pillow>=12.0.0

# Django dependencies
sqlparse>=0.3.0
pytz>=2021.1
asgiref>=3.2.10

# Chatbot (optional - may have dependency conflicts with Python 3.13)
# If installation fails, comment out these lines
# Note: chatterbot may not be compatible with Python 3.13
# chatterbot==1.0.4
# chatterbot-corpus==1.2.0

